# React Native (Expo) Base Template

## Setup Status (macOS)

- Node.js: installed
- Homebrew: installed
- Watchman: installed
- Git: installed
- Xcode: installed (`Xcode 26.2`)
- iOS Simulator Runtime: installed (`iOS 26.2`)
- CocoaPods: installed (`1.16.2`, via Homebrew)

## Validation

- Expo test app created from `create-expo-app`
- `npx expo start --ios` executed successfully
- Metro bundler started and app bundled on iPhone Simulator

## Notes

- This template mirrors `testApp` baseline (without `node_modules`)
- Use `npm install` before first run

## Run

```bash
npm install
npx expo start --ios
```

## Evidence

- Logs: `../setup-logs/`
- Screenshot: `../setup-logs/screenshots/testApp-ios-home.png`
